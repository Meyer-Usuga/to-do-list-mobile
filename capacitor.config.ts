import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meyerusuga.todolist',
  appName: 'to-do-list-mobile',
  webDir: 'www'
};

export default config;
